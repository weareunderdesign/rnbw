export type ToastProps = {
  open: boolean,
  /* children: ReactNode[] | ReactNode,
  open: boolean,
  onClose: () => void, */
}
export type ToastItemProps = {
  description: string,
  title: string,  
  index: number,
}